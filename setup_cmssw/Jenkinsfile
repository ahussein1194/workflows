pipeline {
  agent {
      label 'lxplus'
    }
      environment {
        GITLAB_TOKEN = credentials('GitLabToken')
    }

  stages {
    stage('Setup CMSSW Environment') {
      steps {
	dir('setup_cmssw') {
	script{
	sh './setup_cmssw.sh'
	  }
      	}
      }
    }
  }
}
