pipeline {
  agent {
      label 'lxplus'
    }
      environment {
        GITLAB_TOKEN = credentials('gitlab-apitoken')
    }

  stages {
    stage('Setup CMSSW Environment') {
      steps {
	dir('setup_cmssw') {
	script{
	sh './setup_cmssw.sh'
	  }
      	}
      }
    }
  }
}
