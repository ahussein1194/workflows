/* Requires the Docker Pipeline plugin */
pipeline {
    agent {
        label 'python'
    }
    stages {
        stage('Run Python Script') {
            steps {
                sh 'python3 myScript.py'
            }
        }
    }
}
